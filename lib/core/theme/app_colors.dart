import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(
    0xFFFFF8E5,
  ); // Cream/Beige like in the image

  // Primary
  static const Color primaryOrange = Color(
    0xFFE65100,
  ); // Darker orange for texts/buttons
  static const Color secondaryOrange = Color(0xFFFF9800); // Lighter orange
  static const Color footerOrange = Color(0xFFD84315); // Footer dark orange

  // Text
  static const Color textBlack = Color(0xFF1D1B1E);
  static const Color textGrey = Color(0xFF757575);

  // UI Elements
  static const Color white = Colors.white;
  static const Color greyOutline = Color(0xFFE0E0E0);
  static const Color footerDarkBlue = Color(
    0xFF0F172A,
  ); // Dark blue for bottom footer
}
